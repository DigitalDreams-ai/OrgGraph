{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:runtime": "node ./scripts/dev-runtime.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "info": "tauri info",
    "typecheck": "echo 'desktop shell has no TypeScript sources yet'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "esbuild": "^0.27.3"
  }
}
