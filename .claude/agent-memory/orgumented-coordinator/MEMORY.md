# Orgumented Coordinator Memory

- Keep worker count at three total roles: one coordinator, two workers.
- Default workers: planner and workflow.
- Do not assign overlapping replay-sensitive files to both workers.
